# 项目协作规则

## 项目定位

本项目是“个人风格写作 Agent SaaS”。当前 MVP 核心目标是实现：用户上传合法个人素材，系统建立可编辑的 `Style Profile`，通过 `style_prompt_only + PromptComposer` 生成、改写、审校文章、散文、诗歌和小说内容。`RAG` 不作为首版默认主路径，只作为文章/散文实验增强能力预留。

## 文档职责

- `CONTEXT.md`：维护领域术语、实体关系和业务语言，不记录具体实现方案。
- `00_overview_总览/project_charter.md`：维护项目目标、范围、非目标和成功标准。
- `00_overview_总览/decision_log.md`：记录重要产品、技术、数据、合规和商业决策；每条决策应包含日期、结论和理由。
- `TODO.md`：维护阶段任务、完成状态、阻塞项和下一步；完成工作后同步更新。
- `01_requirements_需求/`：维护产品需求、角色、用户流程和 MVP 范围。
- `02_product_design_产品设计/`：维护页面、交互和用户体验设计。
- `03_technical_solution_技术方案/`：维护架构、模块接口、部署和技术选型。
- `04_prompt_workflow_写作流程/`：维护 Agent 状态机、Prompt 分层和生成策略。
- `05_data_and_style_数据与风格/`：维护数据模型、风格档案、RAG 和隐私隔离规则。
- `06_testing_测试方案/`：维护效果评测、工程测试和上线门槛。
- `07_ops_compliance_运维合规/`：维护采集、版权、内容安全、隐私和公网运维规则。
- `08_business_商业化/`：维护套餐、额度、成本、支付和毛利模型。

## 工作方式

1. 开始实现前先阅读相关目录文档和 `CONTEXT.md`。
2. 先修正根因，保持改动小而集中，不顺手修改无关问题。
3. 新增或改变重要行为时，同步更新对应方案文档、`TODO.md` 和必要的 `decision_log.md` 条目。
4. 不把用户素材、模型密钥、支付密钥或完整原稿写入日志、公开目录或测试输出。
5. 所有用户内容相关查询必须在数据库层执行 `tenant_id` 隔离；不能依赖前端或 Prompt 过滤。
6. 用户素材默认私有且不用于训练；训练、微调或 Personal LoRA 必须单独授权。
7. 采集公开网页时不得绕过登录、付费墙、验证码、访问控制或反爬限制，并记录来源和授权状态。
8. 首版优先使用自研 SaaS 应用层、显式状态机和 PostgreSQL/pgvector；不要无决策地引入 Dify、独立向量库或微调。
9. 当前 `测评集/网页作品` 材料按内部评测和内部 RAG 实验材料处理，跳过授权审查；该例外不自动扩展到训练、微调、公网演示或商业宣传。

## 验证要求

- 修改 Python 工具后运行相邻单元测试。
- 涉及数据隔离、额度、删除、任务恢复、审核或成本记录时，必须补充对应测试或明确记录未覆盖原因。
- 完成阶段任务后更新 `TODO.md` 状态。
- 如果发现现有文档与实现冲突，先记录冲突，再以最新明确决策为准。

## 当前优先级

1. 进入 MVP 工程骨架：Next.js + TipTap 前端、FastAPI 后端、PostgreSQL/pgvector 数据层。
2. 优先实现 `Style Profile` 数据模型、PromptComposer、生成任务状态机和结果版本管理。
3. RAG 只预留接口和实验开关；文章/散文可灰度，小说/诗歌默认关闭。
4. 继续保持用户素材默认私有且不用于训练。
